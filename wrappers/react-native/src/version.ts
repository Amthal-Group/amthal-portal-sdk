/**
 * SDK version reported to the portal in the `init` payload and gated against
 * the manifest's `minSdkVersion`.
 *
 * Keep in sync with package.json "version" — bump both in the same commit.
 */
export const SDK_VERSION = '1.0.0';
